services:
  - type: static
    name: oil-twin-ai
    buildCommand: "echo 'Static site ready - no build step needed'"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
